window.Page={init(){const b=BookService.get(new URLSearchParams(location.search).get("id"));const el=document.getElementById("reader");if(!b){el.innerHTML='<div class="empty card">Không tìm thấy sách.</div>';return}el.innerHTML=`<div style="margin-bottom:20px"><a href="book-detail.html?id=${b.id}">← ${b.title}</a></div><h1>${b.title}</h1><p class="text-muted">${b.author}</p><div class="reader-content" style="margin-top:25px">${b.content.split("\n\n").map(p=>`<p style="margin-bottom:20px">${p}</p>`).join("")}</div>`}};
