window.Page={init(){const ids=Storage.get("library_favorites",[]), books=BookService.all().filter(b=>ids.includes(b.id));document.getElementById("favorite-grid").innerHTML=books.length?books.map(b=>`<article class="card book-card animate-in"><a href="book-detail.html?id=${b.id}"><div class="book-cover"><img src="${b.cover}" alt="${b.title}"></div></a><div class="book-info"><h3>${b.title}</h3><div class="book-meta">${b.author}</div><div class="rating">${Format.stars(b.rating)} ${b.rating}</div></div></article>`).join(""):`<div class="empty card" style="grid-column:1/-1"><div class="empty-icon">♡</div><div class="empty-title">Your library is waiting</div><p class="text-muted">Save books you love and find them here later.</p><a class="btn btn-primary" style="margin-top:18px" href="books.html">Explore Books</a></div>`}};
